export interface IDevice {
    deviceName: string
    model: string
    firmwareVersion: string
    hardwareVersion: string
    deviceUid: string
    status: string,
    ip: string,
    active: boolean
}