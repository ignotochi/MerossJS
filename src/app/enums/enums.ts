export interface Enum {
    [id: number]: string;
}

export enum authActions {
    All = "All",
    token = "token"
}

export enum Menu {
    Home = 'home',
    Login = 'login',
}

export enum Badge {
    Error = 'error',
    Success = 'success',
    Warning = 'warning'
}

